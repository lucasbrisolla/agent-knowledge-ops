# Claude Instructions

Use `AGENTS.md` como fonte canônica de instruções para este repositório.

Não duplique regras de projeto aqui. Mantenha toda orientação compartilhada em `AGENTS.md`.

## Agent skills

### Issue tracker

Issues e specs deste repositório vivem em arquivos Markdown dentro de `.scratch/`. Veja `docs/agents/issue-tracker.md`.

### Triage labels

Use os cinco rótulos canônicos mapeados em `docs/agents/triage-labels.md`.

### Domain docs

Este é um repositório single-context; consulte `CONTEXT.md` na raiz e `docs/adr/` quando existirem. Veja `docs/agents/domain.md`.
